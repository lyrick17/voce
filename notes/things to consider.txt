Things to be updated in Backend
- transfer shell_exec to FlaskApp                                                   - STARTED
    - Python call on spleeter using subprocess 
    - call on ffmpeg using subprocess
- popstate javascript on "Are you sure you want to exit?"
    - window.addEventListener('popstate', function(event) {
            // Check your application state and react accordingly
            console.log('Session history changed:', event.state);
        });
    - // On page load
        if (!sessionStorage.getItem('previousPage')) {
            sessionStorage.setItem('previousPage', window.location.href);
        }

        window.addEventListener('load', function() {
            const previousPage = sessionStorage.getItem('previousPage');
            if (previousPage !== window.location.href) {
                // User navigated from a different page
                sessionStorage.setItem('previousPage', window.location.href);
                console.log('User navigated from:', previousPage);
            }
        });
- reduce file limit from 60 to 25mb                                                - DONE
- check if walang laman ang record file                                            -
- information thingy                                                               -
- list doown all configurations in xampp                                           - DONE
    - max_execution_time = 600 seconds or 10 minutes - in Translator_Functions